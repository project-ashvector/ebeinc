# Codex migration prompt

Inspect this extracted ALLTHINGS140 migration package completely before changing anything. Detect the host OS and run the setup script in dry-run mode. Preserve the Oracle server as the sole broadcast authority and do not restart it merely to configure this PC. Ask me only for passwords/MFA or external account authorization. Install missing supported dependencies, enroll this PC with a unique Tailscale/SSH identity, request access to Cloudflare/GitHub/Drive as needed, restore protected configuration outside Git from `SECRETS-REQUIRED.md`, run the unified diagnostics and tests, verify the public continuous stream and versioned state, verify control access is read-only by default, build Android without changing signing identity, and report every external item still required. Never print secrets or place them in the repository.
