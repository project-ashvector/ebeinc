# Self-hosted migration

Install the same Python runtime and service files on a hardened Linux host, restore SQLite, change only environment/Tunnel/DNS values, validate WSS and backups, then move traffic. Application code contains no Oracle IP or path dependency.
