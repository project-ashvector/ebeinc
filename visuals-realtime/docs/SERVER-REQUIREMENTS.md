# Server requirements

One small Linux VM, Python 3.12+, systemd, outbound HTTPS, loopback listener, Cloudflare Tunnel or an equivalent TLS reverse proxy, and enough disk for a small SQLite database plus 14 compressed daily generations. Radio software, FFmpeg and music storage are neither required nor permitted on this host.
