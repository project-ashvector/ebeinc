{
  "name": "allthings140radio-visuals",
  "private": true,
  "version": "0.2.2",
  "type": "module",
  "scripts": {
    "dev": "tauri dev --config '{\"build\":{\"devUrl\":\"http://127.0.0.1:14340\"}}'",
    "dev:frontend": "vite --host 127.0.0.1 --port 14340",
    "build:frontend": "vite build",
    "build": "npm run build:frontend && tauri build",
    "test": "node --test tests/*.test.mjs",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-dialog": "^2.4.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "vite": "^7.1.3"
  }
}
