# ALLTHINGS140 Visuals 0.1.43 RC3

RC3 supersedes RC2 after hosted configuration verification found the staging service's protected loopback port is 8765. Green takeover scheduling now reads `REALTIME_PORT` from the protected server environment with the existing 8765 deployment as its fallback.

The cycle publisher, Visual renderer contract and application version remain 0.1.43. No Live credential or production routing change is included.
